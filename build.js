// **Task 5.1 — Build a Profile Object**
// Create an object student with these properties: name, age, grade, and isEnrolled.
const student = {
    name: "Alex", 
    age: 22, 
    grade: "B",
    isEnrolled: true
}
// Log the whole object.
console.log(student);
// Log just the name property using dot notation.
console.log(student.name);


// **Task 5.2 — Watch the Video Concept**
// After watching the "What is an Object" video, write 2–3 sentences (as a comment in your code) explaining an object in your own words, using a real-life analogy (not a car or a person — get creative!).
// object is a unique container to store a special variable as key & value pairs. we can store multiple information as property in only one variable. An object can hold multiple pieces of information, just like many real-life things. as like a bookselves. here are some info about the bookshelf, 

const bookshelf = { 
    material: 'wood', 
    numberOfBooks: 200, 
    hasEnglishBooks: true 
} 

// The values of an object can be strings, numbers, booleans, functions, arrays, or even other objects. 
